{
  "name": "foleon-e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:ui": "playwright test --ui",
    "test:auth": "playwright test tests/auth/",
    "test:editor": "playwright test tests/editor/",
    "report": "playwright show-report",
    "report:full": "open custom-report/index.html",
    "report:serve": "node custom-report/serve.js",
    "codegen": "playwright codegen https://app.foleon.com"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/node": "^20.0.0",
    "dotenv": "^16.4.0",
    "monocart-reporter": "^2.10.1",
    "typescript": "^5.4.0"
  }
}
